# C2C Market

面向 Android 学习者的轻量 C2C 电商 / 二手交易应用：买家可浏览**商品**、按**分类**筛选、加入**购物车**并 Mock **下单**；MVP 以**游客**身份使用，不做真支付与登录。

## Language

**商品**：
可浏览、可加入购物车的在售条目；来源为 FakeStore（Remote，id 为正）或本地**挂牌**（LocalListing，id 为负）；统一 `Product` 模型展示。
_Avoid_: 产品（泛指 App 本身时）、SKU

**分类**：
FakeStore 提供的商品类别（如 electronics、jewelery），用于 Tab 筛选与浏览。
_Avoid_: 类目、品类（口语可接受，文档优先用「分类」）

**购物车**：
用户待结算的商品集合；数量与选中状态以 Room 为唯一真相源（SSOT）。
_Avoid_: 购物篮、bag

**收藏**：
用户标记感兴趣的商品；持久化于 Room，与购物车独立。
_Avoid_: 心愿单、喜欢

**浏览历史**：
用户最近打开过的商品详情记录（v2.1）；Room 持久化商品快照与浏览时间，在我的 Tab 展示。
_Avoid_: 搜索历史（输入关键词记录）、足迹

**订单**：
用户提交 Mock 结算后生成的本地交易记录；含商品快照、总价、状态；Room 持久化，无真实支付。
_Avoid_: 交易、买单

**游客**：
未 Google 登录时的默认身份；固定本地 userId `"guest"`，无需注册即可浏览与 Mock 下单。
_Avoid_: 匿名用户、访客

**用户账号**：
App 内的当前身份（v3.2）；可为**游客**或 Google 登录用户；登录后 userId 为 `"google:{sub}"`。
_Avoid_: 账户（多义）、Firebase 用户

**登录**：
通过 Google Sign-In（Credential Manager）将**游客**会话升级为**用户账号**（v3.2）；本地数据一次性迁移归属，无远端账号服务器。
_Avoid_: 注册、OAuth（对用户文案）

**挂牌**：
卖家发布的出售条目（v2.2）；相册选图 + 本地 URI 存 Room，不上传服务器。
_Avoid_: 发布、上架（口语可接受，领域文档用「挂牌」）

**结算**：
从购物车确认商品并生成 Mock 订单的流程；不产生真实扣款。
_Avoid_: 支付、checkout（对用户文案可用「去结算」）

**收货地址**：
用户维护的 Mock 收货信息（v2.3）；Room 持久化，结算时选择并快照写入订单。
_Avoid_: 定位地址、快递单号

**配送提醒**：
Mock 下单成功后由 WorkManager 延迟触发的**本地通知**（v2.4）；非真实物流、非远端 Push。
_Avoid_: 推送、Push 消息

**商品详情**：
单个商品的完整展示页；含图片、描述、价格，可加入购物车或收藏。
_Avoid_: 详情页、product page

**会话**：
围绕某一**商品**的买卖双方私信线程（v3.1）；Room 持久化，含商品快照、卖家信息与最后一条消息摘要。
_Avoid_: 聊天室、频道、群聊

**消息**：
会话内的一条文本内容（v3.1）；含发送者、正文、发送时间与已读状态；离线未发送内容以**草稿**形式存 Room。
_Avoid_: 推送、系统通知

**联系卖家**：
从**商品详情**发起私信、进入或创建对应**会话**的操作（v3.1）；FakeStore **商品**使用 Mock 卖家；**挂牌**卖家与当前**用户账号**相同时不显示此入口。
_Avoid_: 在线客服、评论

**深度链接**：
形如 `c2cmarket://product/{id}` 的 URI，从通知、分享或外部浏览器唤起 App 并导航至对应页面（v3.3）。
_Avoid_: App Link、Universal Link（HTTPS 域验证）

**面交地点**：
**挂牌**可选填写的线下交易位置描述（v3.3）；文本存 Room，可通过系统地图 Intent 查看，非 GPS 坐标。
_Avoid_: 定位、导航、Maps SDK
